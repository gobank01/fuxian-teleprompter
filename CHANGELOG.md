# Changelog

## 1.1.0 — 2026-09-21

### เพิ่ม

- นำเข้าสคริปต์จาก `.txt`, `.md` และไฟล์สำรอง `.json` พร้อมตรวจรูปแบบและขนาดก่อนนำเข้า
- ส่งออกสคริปต์ที่เลือกเป็น `.txt` และสำรองคลังทั้งหมดเป็น `.json` ที่ระบุเวอร์ชันรูปแบบข้อมูล
- รวมรายการนำเข้าโดยรักษารายการเดิม ข้ามรายการที่ชื่อและข้อความตรงกัน และเก็บรายการชื่อเหมือนแต่เนื้อหาต่างกันแยกไว้
- ทำสำเนาสคริปต์ แสดงจำนวนอักขระ/ท่อน และสถานะการบันทึก
- จำความเร็ว ขนาดตัวอักษร กระจก การสุ่มท่อน การวนซ้ำ การนับถอยหลัง และการจัดข้อความในเบราว์เซอร์
- เลือกเปิด/ปิดสุ่มท่อนและวนซ้ำ นับถอยหลัง 0 / 3 / 5 วินาที และจัดข้อความกึ่งกลาง/ชิดซ้าย
- คู่มือในแอปและการปรับหน้าจอสำหรับมือถือ
- คำสั่ง `npm test`, `npm run test:unit`, `npm run test:browser` และ GitHub Actions สำหรับทดสอบอัตโนมัติโดยไม่เพิ่ม dependency

### ปรับปรุงเอกสาร

- คู่มือภาษาไทยสำหรับเริ่มใช้สคริปต์ทั่วไป ดาวน์โหลดซอร์ส เปิด local server และย้ายคลังด้วยไฟล์สำรอง
- อธิบายการเก็บข้อมูลในเบราว์เซอร์ การไม่ซิงก์ข้ามอุปกรณ์ และข้อจำกัดของตามเสียง/หน้าต่างลอย
- ระบุขอบเขตการคัดกรองคำเสี่ยงและแยกราคาในสคริปต์ตัวอย่างออกจากข้อมูลปัจจุบันที่ผู้ใช้ต้องตรวจสอบ

### ความเข้ากันได้

- เก็บคลังตัวอย่าง Fuxian 100 ท่อนและเส้นทาง migration ของ seed เดิมไว้
- ใช้ข้อมูลเดิมใน `fx_scripts`, `fx_cur`, `fx_seed_v` ต่อ และเพิ่มค่าการอ่านที่ `fx_settings_v1`
- ไม่มีบัญชีผู้ใช้ backend หรือการซิงก์อัตโนมัติ การนำเข้าเพิ่มข้อมูลในเบราว์เซอร์ปัจจุบัน
- ไฟล์สำรอง JSON รองรับ 32 MiB และรักษาชื่อ/ข้อความเดิม รวมชื่อว่าง ช่องว่างและ BOM; TXT/MD/JSON อาร์เรย์รุ่นเก่าคงขีดจำกัด 2 MiB และ 200 สคริปต์ต่อไฟล์
- แก้โหมดเสียงที่ยังขึ้นรอบใหม่หลังผู้ใช้กดพัก และกลับมาอ่านต่อจากคำเดิมได้

## ก่อน 1.1.0

แอป static สำหรับสคริปต์ Fuxian พร้อมคลัง Multi-Oil LIVE 100 ท่อน, seed migration รุ่น 8, การคัดกรองคำเสี่ยง, เลื่อนอัตโนมัติ, ตามเสียงภาษาไทย, กระจก, หน้าต่างลอย และชุดทดสอบ seed/migration รายละเอียดและข้อควรระวังในการพัฒนาต่ออยู่ใน [README](README.md)
